{
  "name": "product-factory",
  "displayName": "Product Factory Pipeline",
  "version": "1.7.0",
  "description": "End-to-end product delivery pipeline: discovery -> spec -> plan -> gated approval -> implement -> test -> review -> handoff -> deploy (optional). 19 skills (10 pipeline + 9 bundled dependencies, CI scaffolding included and off by default) + 5 agents, portable across projects."
}
