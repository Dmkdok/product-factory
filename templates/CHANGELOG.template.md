# Changelog

Append-only, newest entry on top. One entry per real ship — a `deploy-product` run, or an
`iterate-product` milestone close for a local-only product with no deploy target. Not every commit
and not every session; this is the answer to "what changed, in order," not a commit log.

## Unreleased
-

<!--
## <version or date> — <one-line summary>
- What changed, user-visible first.
- Link the commit/version from `docs/RELEASE.md` if this shipped via deploy-product.
-->
